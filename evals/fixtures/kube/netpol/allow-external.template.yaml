apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: offlinegpt-den-allow-eval-mock-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: offlinegpt-ee
      app.kubernetes.io/instance: offlinegpt-ee
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - den-api
          - den-web
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: ${OFFLINEGPT_EVAL_KUBE_ALLOWED_HOST_IP}/32
      ports:
        - protocol: TCP
          port: ${OFFLINEGPT_EVAL_KUBE_ALLOWED_MOCK_PORT}
