apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: offlinegpt-den-default-deny-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: offlinegpt-ee
      app.kubernetes.io/instance: offlinegpt-ee
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - den-api
          - den-web
  policyTypes:
    - Egress
  egress: []
