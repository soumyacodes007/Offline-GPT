apiVersion: v1
kind: Secret
metadata:
  name: offlinegpt-mysql
  labels:
    app.kubernetes.io/name: offlinegpt-mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: password
  MYSQL_DATABASE: offlinegpt_den
  MYSQL_USER: offlinegpt
  MYSQL_PASSWORD: offlinegpt
---
apiVersion: v1
kind: Service
metadata:
  name: offlinegpt-mysql
  labels:
    app.kubernetes.io/name: offlinegpt-mysql
spec:
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app.kubernetes.io/name: offlinegpt-mysql
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: offlinegpt-mysql
  labels:
    app.kubernetes.io/name: offlinegpt-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: offlinegpt-mysql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: offlinegpt-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          imagePullPolicy: IfNotPresent
          ports:
            - name: mysql
              containerPort: 3306
          envFrom:
            - secretRef:
                name: offlinegpt-mysql
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command:
                - sh
                - -lc
                - mysqladmin ping -h 127.0.0.1 -uofflinegpt -p"$MYSQL_PASSWORD"
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24
      volumes:
        - name: data
          emptyDir: {}
