extends: ./electron-builder.base.yml
appId: com.differentai.offlinegpt
productName: OfflineGPT
protocols:
  - name: OfflineGPT
    schemes:
      - offlinegpt
linux:
  executableName: offlinegpt
artifactName: offlinegpt-${os}-${arch}-${version}.${ext}
