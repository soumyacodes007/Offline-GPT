extends: ./electron-builder.base.yml
appId: com.differentai.offlinegpt
productName: OfflineGPT Enterprise
extraMetadata:
  offlinegptDistribution: enterprise
protocols:
  - name: OfflineGPT Enterprise
    schemes:
      - offlinegpt
publish:
  - provider: github
    owner: different-ai
    repo: offlinegpt
    releaseType: release
    channel: enterprise
linux:
  executableName: offlinegpt-enterprise
artifactName: offlinegpt-enterprise-${os}-${arch}-${version}.${ext}
