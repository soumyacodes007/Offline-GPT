---
name: browser-setup
description: Guide user through OfflineGPT Browser automation setup
---

Help the user set up browser automation.

Tell the user that OfflineGPT Browser is built in and enabled by default.
Keep the user prompt minimal, then use browser tools against the built-in browser target when they ask to browse.
