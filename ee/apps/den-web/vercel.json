{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "turbo query affected --base=$VERCEL_GIT_PREVIOUS_SHA --packages @offlinegpt-ee/den-web --exit-code || exit 1",
  "installCommand": "cd ../../.. && pnpm install --frozen-lockfile --filter @offlinegpt-ee/den-web..."
}
